{
  "name": "vedaai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend && npm install",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently --kill-others \"npm run start --prefix backend\" \"npm run start --prefix frontend\" \"node proxy.js\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "http-proxy": "^1.18.1"
  }
}
